import {  getBoard } from "./board";

export function movePiece(from: string, to: string ): boolean{
    // let move: Move;

    // // check for normal move 
    // if (moveRegex.test(input)){
    //     //const match = 
    // }
    return false;
}